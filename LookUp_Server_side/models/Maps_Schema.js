var mongoose  = require('mongoose');
var schema = mongoose.Schema;   

var Maps = new mongoose.Schema({
      
    
    });

var mapSchema = mongoose.model('Favorite',Maps);
module.exports = mapSchema;